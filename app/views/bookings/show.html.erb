    <h1>Booking</h1>
<div class="container d-flex">
  <div class="booking-product">
    <div class="booking-card">
      <h2> Confirmation number: <%= @booking.id %></h2>
      <p>Start : <%= @booking.reservation_begin %></p>
      <p>End : <%= @booking.reservation_end %></p>
      <p>Price per day : <%= @booking.transport.price %> €</p>
    </div>
    <div class="booking-picture">
      <h2>Transport details :</h2>
        <% if @booking.transport.photos.attached? %>
          <% @booking.transport.photos.each do |photo| %>
            <%= cl_image_tag photo.key, :width=>180, :height=>200, crop: :fill %>
          <% end %>
        <% else %>
          <%= 'No Image' %>
        <% end %>
    </div>
    <div class="booking-description">
      <h2> Brand : <%= @booking.transport.brand %></h2>
      <h2> Power : <%= @booking.transport.horse %></h2>
      <h2> Year : <%= @booking.transport.year %></h2>
      <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Vero, ipsam repellat, rerum modi explicabo odio ab iste, accusantium dolore saepe eveniet est magni porro animi libero in magnam facere earum.</p>
    </div>
  </div>
</div>
  <div class="containers">
  <div class="btn-home"><%= link_to "Home", root_path(@transports), class: "btn btn-primary" %><br /></div>
  <div class="btn-home"><%= link_to 'Edit', edit_booking_path(@booking.id) %></div>

  </div>
